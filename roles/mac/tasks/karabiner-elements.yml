---

- name: Install karabiner-elements
  homebrew_cask:
    name: karabiner-elements
    state: present

- name: Config karabiner-elements
  copy:
    src: karabiner.json
    dest: "{{ ansible_user_dir }}/.config/karabiner/"
