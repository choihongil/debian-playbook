---

- name: Install packages
  apt:
    name: "{{ item }}"
    state: latest
  with_items:
    - cifs-utils
    - curl
    - docker-engine
    - exuberant-ctags
    - fish
    - fonts-powerline
    - man-db
    - nodejs
    - openssh-server
    - python3-pip
    - vim-nox
    - tmux
    - yarn
  become: yes

- name: Install pip packages
  pip:
    name: "{{ item }}"
    executable: pip3
  with_items:
    - docker-compose
    - mps-youtube
    - powerline-status
    - ranger-fm
    - youtube_dl
  become: yes
