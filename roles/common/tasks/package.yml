---

- name: Install packages
  apt:
    name: "{{ item }}"
    state: latest
  with_items:
    - cifs-utils
    - curl
    - docker-engine
    - exuberant-ctags
    - man-db
    - nodejs
    - powerline
    - ranger
    - vim-nox
    - tmux
    - zsh
  become: yes
